<main>

  <section id="catalog">
    <h1>All posts</h1>
    <div class="band">
      {{#each creatures}}
      {{>creatureTempl}}
      {{/each}}
      <!-- If there are still no posts in the database display: -->
    {{#if empty}}
      <div class="no-posts">
        <p class="no-offer">There are no posts yet...</p>
      </div>
    {{/if}}

    </div>
  </section>
</main>